/// Plain Dart model for a configurable XP threshold per level.
class LevelConfigModel {
  final int level;
  final int xpRequired;

  const LevelConfigModel({required this.level, required this.xpRequired});
}